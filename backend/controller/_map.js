export const getAllMaps = (req, res) => {
  res.send("Hi i am the controller that fetches all the maps");
};

export const deleteAllMaps = (req, res) => {
  res.send("<h1>deleted all the maps</h1>");
};
